# dm-chat

to install do ```npm install```

to use docker do
```
git clone https://github.com/test9141/dm-chat
cp ./dm-chat/docker/docker-compose.yml ./
cp ./dm-chat/docker/Dockerfile ./
docker compose up -d
```
